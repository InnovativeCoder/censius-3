import React from 'react'

export default function Card({image, name, position, emailId}) {
    return (
        <div>
            Card
            {
                name
            }
        </div>
    )
}
